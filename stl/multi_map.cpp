#include <bits/stdc++.h>
using namespace std;

int main() {
  // multimaps are similar to maps, difference being you can store duplicate keys also
  // SORTED order
  multimap<int, int> up;

  return 0;
}
