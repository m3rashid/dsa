#include <bits/stdc++.h>
using namespace std;

int main() {
  // map O(lon(n))
  // unordered_map O(1), worst case: O(N)

  unordered_map<int, int> m;
  // same as set and unordered_set

  return 0;
}
