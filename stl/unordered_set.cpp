#include <bits/stdc++.h>
using namespace std;

int main() {
  unordered_set<int> us;
  // lower_bound and upper_bound functions dont work, all the other functions are same
  // stores unique elements, but not in sorted order

  // time complexity is O(1) in most of the functions. But in worst case, goes to O(N). Worst case is not seen often, once in a blue moon

  return 0;
}
